---
status: draft
phase: 1
owners: [@rileydrakedesign]
last-reviewed: 2026-04-29
---
<!-- last-reviewed bump on 2026-04-29 covers both the doc-bundle landing and the codegen-pipeline pre-flight batch landed the same day. -->


# Phase 1 — Decisions

Phase 1 decision log. Append-only. Cross-phase decisions stay in [`../../../project-context.md` §9.5](../../../project-context.md#95-decision-log); decisions scoped to Phase 1 are recorded here.

Format: `YYYY-MM-DD — Short title.` followed by decision, rationale, alternatives considered, cost or trade-off, owner.

---

## Entries

- **2026-05-04 — Codegen pipeline ships, all three runners green (46/46).** First end-to-end codegen run completed. Pipeline restructured from per-file to one-shot generation: `protocol/schemas/codegen_export.cue` exposes the canonical 18 wire-format definitions as concrete fields under `CodegenRoot`, `cue def --out=jsonschema -e CodegenRoot ./protocol/schemas/` produces a single `_generated/all.json` with `$defs`, then a Python post-processing step normalizes CUE's `#Foo` keys / `%23Foo` refs to plain `Foo` (typify and ajv reject URL-encoded `$ref`s). Schema fixes required to reach 46/46: (a) moved `import "list"` to top-of-file in five schemas (CUE syntax requires it), (b) replaced `params?: {...}` / `result: {...}` with `params?: [string]: _` / `result: [string]: _` in `#Request`/`#Notification`/`#Response` because CUE exports `{...}` as `{const: {}}` (the empty-object literal — wrong), (c) dropped the `#Hunk.end_line >= start_line` cross-field constraint (JSON Schema can't express it; daemon enforces; inverted-range fixture renamed `valid_hunk_inverted_range_daemon_layer.json`), (d) `#WorktreePath` uses `\x00` instead of `\0` (Rust regex crate doesn't recognize `\0` in char classes), (e) `#FilePath` upper bound `{0,1023}` reduced to `{0,999}` (Go's RE2 rejects bounded counts ≥ 1000), (f) `negative_detail_overlength` fixture data lengthened from 248 to 257 chars (the original was within the 256-char bound and never actually triggered the violation). Forced jsonschema 0.17 (Rust) into `Draft202012` mode via feature flag and `with_draft` so `$ref` siblings (`properties`/`required`/etc.) are honored — without this, derived schemas silently inherited only the loose base via $ref and dropped their tightening constraints. `cargo-typify` 0.6.2 panics on the `allOf` + `not` patterns from `#FilePath`/`#BranchName` and on the type-narrowing `allOf` for `#LineNumber`/`#TtlSeconds`; Rust generated types are emitted as a stub explaining the situation. Phase 1 conformance does not require Rust generated types — the runner validates against JSON Schema directly — so this is deferred. Codegen verified idempotent (md5 of generated tree equal across consecutive runs). Owner: @rileydrakedesign. Closes follow-ups #12 and #13; #14 (cargo-typify) and #15 (rules_rust crate-universe) remain deferred.
- **2026-04-29 — Codegen pipeline pre-flight: schemas array + runner upgrade + CI extension.** Pre-staged the work needed to take the codegen pipeline from "schemas exist on disk" to "runners validate fixtures against `schema_ref` per [`../../CONFORMANCE.md`](../../CONFORMANCE.md) §4 in all three languages." Concretely: (a) [`tools/codegen/generate.sh`](../../../tools/codegen/generate.sh) `SCHEMAS` array extended from 3 → 14 entries (added `common`, all lifecycle / awareness / conflict schemas added in the 2026-04-29 wire-format batch); (b) all three conformance runners (`crates/tap-protocol/tests/conformance.rs`, `pkg/protocol/conformance_test.go`, `packages/tap-protocol/src/test/conformance.test.ts`) rewritten to load JSON Schemas from `protocol/schemas/_generated/`, validate each fixture's `data` against `schema_ref`, emit the [`../../CONFORMANCE.md`](../../CONFORMANCE.md) §4.1 JSON output, and exit per §4.2; validators added (`jsonschema 0.17` for Rust, `santhosh-tekuri/jsonschema/v6` for Go, `ajv 8` for TypeScript); (c) Bazel `data` deps wired from [`../../../crates/tap-protocol/BUILD.bazel`](../../../crates/tap-protocol/BUILD.bazel) and [`../../../pkg/protocol/BUILD.bazel`](../../../pkg/protocol/BUILD.bazel) test rules to `//protocol/conformance:fixtures`, `//protocol/conformance:version`, and the new `//protocol/schemas:generated_schemas` filegroup; runners detect Bazel runfiles via `TEST_SRCDIR` with cargo / `go test` fallbacks; Go conformance test added to `srcs` of the `go_test` rule (was missing); (d) [`../../../.github/workflows/ci.yml`](../../../.github/workflows/ci.yml) `codegen-drift` job extended with `typify-cli` (cached) and `json-schema-to-typescript` install steps; `conformance` job switched from the `//protocol/conformance/...` filegroup target (vacuously green) to the explicit per-language runners; new `typescript` job runs `pnpm --filter @tap/protocol test` because the TS Bazel `js_test` target is deferred; (e) [`../../../protocol/conformance/VERSION`](../../../protocol/conformance/VERSION) created (`0.1.0`), closing Q-1-002; (f) five sandboxing fixtures with literal control bytes (NUL, BEL, ESC, DEL, RTL) re-encoded with `\\uXXXX` JSON escapes so the fixture files themselves are RFC 8259-conformant — decoded strings still carry the actual control codepoints so the schema regex still rejects them. Owner: @rileydrakedesign. Out of scope and deferred: Bazel `js_test` for TS (needs `npm_translate_lock`), Q-1-001 canonicalization rule (runners use stable serializer + sorted keys), Go re-export wrapper for generated types (needs concrete type names from first codegen run), the 5 response-type negative fixtures (coverage debt; tracked separately).
- **2026-04-29 — Phase 1 doc bundle stood up.** Phase 1 doc bundle ([`README.md`](README.md), [`test-plan.md`](test-plan.md), [`perf-budget.md`](perf-budget.md), [`threat-model.md`](threat-model.md), [`migration.md`](migration.md), [`open-questions.md`](open-questions.md), this file, [`exit.md`](exit.md)) lands ahead of the wire-format spec finishing. Rationale: the spine is template-derived and does not depend on spec progress; landing it now exercises the [`../../DOCUMENTATION_PLAN.md`](../../DOCUMENTATION_PLAN.md) machinery before Phase 2 starts. Owner: @rileydrakedesign.

(append new entries above; newest first)
